# taco-server

Separated repo for the TacoClaw public dashboard stack.

Contents:
- `dashboard/` Node gateway serving the public UI and proxying live API routes
- `api/` legacy Flask API server files still used by local services
- `terminal/` terminal helper scripts used by the TacoClaw mobile shell
