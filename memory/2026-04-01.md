# Memory — 2026-04-01

## Engine Fixes Deployment (Fixes 1-8)

### Changes Deployed:
- **Fix 1:** SIGNAL_MAX_ENTRY_PRICE raised 0.68 → 0.82 (unblocks high-conviction signals)
- **Fix 2:** MAKER_OFFSET reduced 0.003 → 0.001 (more competitive limit orders)
- **Fix 3:** BTC price trimming bug fixed (1h trend filter now gets proper 70min data)
- **Fix 4:** Momentum deceleration threshold 0.5 → 0.3 (fewer false negatives)
- **Fix 5:** Maker retry after cancellation (>45s remaining)
- **Fix 6:** FOK taker fallback after 60s (MAKER_FOK_FALLBACK_SEC)
- **Fix 7:** Reduced arb log noise (logs once per window)
- **Fix 8:** Fill rate diagnostics added (diagnoses fill/cancel per window)

### Files Replaced:
- `scripts/polymarket_btc15m.py` → backed up with timestamp
- `scripts/polymarket_eth15m.py` → backed up with timestamp
- New files include "IMPROVED" header with all 8 fixes listed

### Config Updates (secrets.env):
- Cleared `BTC15M_SIGNAL_MAX_ENTRY_PRICE` → using new default 0.82
- Cleared `ETH15M_SIGNAL_MAX_ENTRY_PRICE` → using new default 0.82
- Cleared `ETH15M_SNIPE_MAX_PRICE` → using new default 0.82
- Updated `BTC15M_MAKER_OFFSET` → 0.001
- Updated `ETH15M_MAKER_OFFSET` → 0.001

### Verification Status:
✅ Files successfully replaced in scripts/
✅ Config updated in secrets.env
✅ Partial evidence of new code running:
  - Fix 4: Momentum threshold 0.3 confirmed in logs (`threshold=0.3)`)
  - Fix 8: Fill rate diagnostics confirmed (`[DIAG] Window fill rate`)
  - High-price trade detected: Order at 0.8100 (above old max 0.68)
⚠️ Processes need full restart:
  - Log file not rotated — shows STARTING message from yesterday with old config
  - Old values still in startup banner (offset=0.003, max_entry=0.68)
  - Process loaded old .py file into memory yesterday and hasn't been restarted

### Scripts Created:
- `restart_engines.sh` — Force-kill and restart both engines
- `verify_fixes.py` — Verification script to check which fixes are active

### Next Action Required:
Run on Pi to fully load all fixes:
```bash
cd ~/.openclaw/workspace/trading
./restart_engines.sh
```

## TTS Status
- Issue: Microsoft Edge TTS not registering provider
- Error: `microsoft: no provider registered`
- Cause: `node-edge-tts` installed but OpenClaw not finding it
- Tried: Install in `/usr/lib/node_modules/openclaw/node_modules`
- Tried: Install in global npm directory
- Current workaround: Using OpenAI TTS (but quota exhausted, 429 error)
- OpenAI status: Configured but hit API quota limit
- User requested: Try `github.com/andresayac/edge-tts` (Python library)
- Note: OpenClaw uses npm packages, not Python for built-in TTS
- Email logs export attempted: Script created and JSON exported successfully

## Cloudflare Tunnel
- Quick tunnel URL: `https://decorative-polar-smithsonian-polls.trycloudflare.com`
- Tunnel endpoint: `localhost:7681` (ttyd)
- Named tunnel: "taco" (installed as systemd service)
- DuckDNS domain: `taco-pi.duckdns.org` (created, needs Cloudflare DNS integration)

## Trading Activity
- BTC maker orders: Multiple orders placed and cancelled due to time constraints
- ETH maker orders: Active, some fills confirmed
- Auto-redeem: Working and catching wins
- Today's performance: BTC down, ETH mixed, redeems positive
# Memory — 2026-04-01

## Engine Fixes Deployment (Fixes 1-8)

### Changes Deployed:
- **Fix 1:** SIGNAL_MAX_ENTRY_PRICE raised 0.68 → 0.82 (unblocks high-conviction signals)
- **Fix 2:** MAKER_OFFSET reduced 0.003 → 0.001 (more competitive limit orders)
- **Fix 3:** BTC price trimming bug fixed (1h trend filter now gets proper 70min data)
- **Fix 4:** Momentum deceleration threshold 0.5 → 0.3 (fewer false negatives)
- **Fix 5:** Maker retry after cancellation (>45s remaining)
- **Fix 6:** FOK taker fallback after 60s (MAKER_FOK_FALLBACK_SEC)
- **Fix 7:** Reduced arb log noise (logs once per window)
- **Fix 8:** Fill rate diagnostics added (diagnoses fill/cancel per window)

### Files Replaced:
- `scripts/polymarket_btc15m.py` → backed up with timestamp
- `scripts/polymarket_eth15m.py` → backed up with timestamp
- New files include "IMPROVED" header with all 8 fixes listed

### Config Updates (secrets.env):
- Cleared `BTC15M_SIGNAL_MAX_ENTRY_PRICE` → using new default 0.82
- Cleared `ETH15M_SIGNAL_MAX_ENTRY_PRICE` → using new default 0.82
- Cleared `ETH15M_SNIPE_MAX_PRICE` → using new default 0.82
- Updated `BTC15M_MAKER_OFFSET` → 0.001
- Updated `ETH15M_MAKER_OFFSET` → 0.001

### Verification Status:
✅ Files successfully replaced in scripts/
✅ Config updated in secrets.env
✅ Engines fully restarted (~15:49 EDT) — new config confirmed in logs:
  - BTC: `max_entry=0.82 offset=0.001`
  - ETH: `max_entry=0.82 offset=0.001`
✅ Watchdogs restarted (`polymarket_15m_watchdog.sh` + `watchdog.sh`)

### Scripts Created:
- `restart_engines.sh` — Force-kill and restart both engines
- `verify_fixes.py` — Verification script to check which fixes are active

## Second Script Update (~16:36 EDT)
- User sent two new files via Telegram:
  - `polymarket_btc15m---b61c37b8...` → replaced `scripts/polymarket_btc15m.py`
  - `polymarket_eth15m---756fb938...` → replaced `scripts/polymarket_eth15m.py`
- MD5 hashes verified — files match
- Engines need restart to load second update
- Quick tunnel URL provided by user: `https://degrees-aluminum-equilibrium-rarely.trycloudflare.com`

## TTS Status
- Microsoft Edge TTS: `no provider registered` — broken
- OpenAI TTS: Configured but quota exhausted (429)
- TTS effectively non-functional

## Tailscale
- **ENABLED** at ~16:19 EDT (was previously disabled)
- Pi Tailscale IP: `100.110.239.77`
- Hostname: `raspberrypi.tail24713d.ts.net`
- iPhone on same network: `100.81.7.37`
- ttyd running on port **7682** (port 7681 occupied by OpenClaw gateway)
- Terminal access: `http://raspberrypi.tail24713d.ts.net:7682` or `http://100.110.239.77:7682`

## Cloudflare Tunnel
- Named tunnel "taco": systemd service, no public hostname in config
- Quick tunnel URLs are ephemeral; latest: `https://degrees-aluminum-equilibrium-rarely.trycloudflare.com`
- DuckDNS: `taco-pi.duckdns.org` exists but not wired up

## Trading Activity (2026-04-01)
- BTC-15m and ETH-15m both live with all 8 fixes
- Multiple maker fills confirmed during afternoon session
- Tailscale now permanent remote access method
