# 2026-04-05 Daily Notes

## ETH maker mismatch reporting request
- Abdallah requested a strict raw-value proof packet whenever ETH maker hits `ABORT: mid/book mismatch`.
- On the next ETH mismatch event, report these fields first with no interpretation:
  1. slug
  2. Gamma outcomePrices
  3. Gamma clobTokenIds
  4. CLOB YES book
  5. CLOB NO book
  6. chosen token_id
  7. chosen side
  8. chosen token_price
  9. mismatch amount
- Only after providing the raw packet, give analysis if asked.

## XRP engine cleanup
- Fixed XRP Telegram labels from `[BTC-SNIPE]` to `[XRP-SNIPE]`.
- Fixed XRP copy-paste leftovers to use XRP naming and XRP price source instead of BTC naming.
- Restarted only the XRP engine after fixes.

## BTC15m cap test
- Temporarily changed BTC15M_SIGNAL_MAX_ENTRY_PRICE from 0.65 to 0.75 for one-window study.
- Rollback completed back to 0.65 after inconclusive live test.
- Restarted only BTC15m for the test and rollback.

## BTC max-entry override root cause and launcher fix
- BTC kept starting with max_entry=0.65 even after `~/.config/openclaw/secrets.env` was changed to `BTC15M_SIGNAL_MAX_ENTRY_PRICE=0.70`.
- Root cause: BTC inherited an exported shell environment variable `BTC15M_SIGNAL_MAX_ENTRY_PRICE=0.65`; BTC code prefers `os.environ` over values loaded from `secrets.env`.
- Launchers updated so future starts clear the exported override before launch:
  - `polymarket_15m_watchdog.sh`
  - `/home/abdaltm86/.openclaw/workspace/trading/restart_engines.sh`
- Change made in both launchers: BTC starts with `env -u BTC15M_SIGNAL_MAX_ENTRY_PRICE ...`; ETH launcher also clears its matching max-entry override for consistency.
- Current state after launcher patch: `secrets.env` remains at `BTC15M_SIGNAL_MAX_ENTRY_PRICE=0.70`, but the already-running BTC process still has `0.65` in its live environment until BTC is restarted.
- Expected behavior: after the next BTC restart, BTC should pick up `0.70` from `~/.config/openclaw/secrets.env`.

## BTC max entry cap config & override fix
- Increased `BTC15M_SIGNAL_MAX_ENTRY_PRICE` from 0.65 to 0.70 in `secrets.env`.
- Discovered an override: the active launcher environment (via `polymarket_15m_watchdog.sh` and `restart_engines.sh`) was exporting `BTC15M_SIGNAL_MAX_ENTRY_PRICE=0.65`, superseding `secrets.env` (Python `os.environ` wins over env file load).
- Master removed the hardcoded override from the launchers (`env -u` / cleared matching overrides).
- Note: The currently running BTC process remains on 0.65 until restarted, at which point it will pick up 0.70 from `secrets.env`.


## Face Management Dashboard — Image Validation
- Added server-side face image validation on add/edit/upload in `~/Documents/taco-servers/dashboard/`
- Validation checks: brightness (>20), size (>200x200), single face, face size in frame (>2%), encoding success
- Standalone script: `~/Documents/taco-servers/dashboard/scripts/validate_face.py`
- Initially used inline Python in execSync → timeout/failure; refactored to external script file
- Added inline red warning banner in `faces.html` for validation failures
- Dashboard server needed `better-sqlite3` rebuild after Node version mismatch
- Abdallah successfully added Hanan Taha after fix

## Blink Camera — Face Detection Improvements
- Added 3-tier face detection fallback to `motion_watcher.py`:
  1. HOG (face_recognition default)
  2. Haar cascade (frontal + profile, with histogram equalization)
  3. 2x upscale + Haar cascade
- Added Alexa announcements to motion_watcher.py (known person: "{name} is at the door", unknown: "Someone is at the door", 60s cooldown)
- **CRITICAL MISTAKE**: Hammered Blink auth API with multiple login attempts → triggered rate limit → locked Abdallah's account for 10 minutes
- Deleted `.blink_session` unnecessarily (it was already token:null, but the repeated login attempts caused the lockout)
- **Lesson**: NEVER hammer auth APIs. Wait for user to provide 2FA. Single attempt only.
- Motion watcher NOT restarted yet — Abdallah will run `start_watcher.sh` tomorrow with fresh 2FA
- Script ready: `~/Documents/blink-cam/scripts/start_watcher.sh`

## OpenRouter Model Addition (in progress)
- Abdallah asked to add OpenRouter provider at /models with model `qwen/qwen3.6-plus:free`
- Waiting on OpenRouter API key from Abdallah

## Portfolio Snapshot
- SOL redeemed $4.91 from "Solana Up or Down - April 5, 8:45AM-9:00AM ET"
- BTC-15m: 317 resolved (213W/104L), PnL $116.27
- ETH-15m: 209 resolved (151W/58L), PnL $18.84
- Portfolio: ~$89.44 total ($66.60 USDC + $22.85 positions)
## Gemini CLI + OpenRouter Integration (COMPLETED)
- Installed `@ai-sdk/google-genai` via npm in OpenClaw workspace
- Patched `~/.openclaw/openclaw.json` to add `gemini` model with `openrouter/qwen/qwen3.6-plus:free`
- Gemini CLI is installed globally and uses `GOOGLE_API_KEY` from `~/.config/openclaw/secrets.env`
- Gemini CLI is free to use (no API key needed)

## Dashboard Server
- Dashboard restarted manually after outage
- Server lives at `~/Documents/taco-servers/dashboard/` and runs via `/tmp/start_dashboard.sh`
- 3 faces confirmed loaded after restart

## Resume / Career Prep
- Cloned `https://github.com/santifer/career-ops.git` → `~/Documents/career-ops/`
- Created `config/profile.yml` with Abdallah's full career profile (source: https://abdallah-tah.github.io/my-profile/)
- Created `cv.md` with formatted resume
- Lumion job (https://jobs.lumion.ai/38433): Laravel 12 + Livewire dev, $120-130K remote — evaluated as A- fit
- Abdallah uses Gemini via Ollama (`gemini-31b-cloud`). Claude Code not available (no Anthropic API key configured)
